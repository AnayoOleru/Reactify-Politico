import React from 'react';
import { AllOffice} from '../src/components/pages/office-page.jsx';
import { shallow, mount} from 'enzyme';
import jest from 'jest';

describe('Office component', () => {
    // const props = {
    //     getAllOffice: jest.fn(),
    //     success: true,
    //     history: {
    //       push: '/',
    //     },
    //   };
    //  it('render component', () => {
    //      const wrapper = shallow(<AllOffice {...props} />);
    //      const  signupState  = (wrapper.instance().state.name).toEqual('');
    //  });
});
